# SmartMed Agent 服务 - 运行依赖（09 ticket）
# 安装: python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt
# LLM 接入统一 OpenAI-compatible 接口（CONTEXT §5），仅依赖 langchain-openai 一家 SDK

fastapi>=0.111
uvicorn>=0.30
pydantic-settings>=2.3
langgraph>=0.2.30
langchain-openai>=0.1.20
httpx>=0.27
