# 05 — 挂号全链路（含并发控制）

**What to build:** 患者确认挂号后，系统通过 Redis Lua 脚本原子扣减号源，成功后生成挂号凭证；号源不足时提示已抢完；同一用户 5 秒内重复请求被拒绝。草稿 30 分钟过期自动作废。

**Blocked by:** 04 — 排班与号源管理

**Status:** done

- [x] 后端：挂号草稿生成接口（校验排班有效性，写入 Redis TTL 30min，不扣号源）
- [x] 后端：挂号确认接口（消费草稿 → Lua 原子扣减 → 写 PG 挂号记录 → 返回凭证）
- [x] Redis Lua 脚本：检查 remaining > 0 → 扣减 1 → 返回成功/失败
- [x] 防刷：同用户同排班 5 秒内仅允许一次确认请求（Redis 频率限制）
- [x] 草稿不可重复消费（消费后删除 key）
- [x] 后端：取消挂号接口（就诊前 2 小时以上可取消，号源释放回 Redis + PG）
- [x] 后端：挂号记录查询 API（列表 + 详情，按状态筛选）
- [x] 集成测试：并发扣减不超卖、防刷拦截、草稿过期作废
